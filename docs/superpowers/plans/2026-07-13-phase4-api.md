# Phase 4 — API consolidation + mixin split; cut 0.2.0 (final phase of the breaking series)

Tracking: glinet4/glinet4#16 (phase) / #19 (umbrella). Source: ~/dev/oss/GLINET4-TECHDEBT.md Phase 4. Context: glinet.py is ~800 lines / 60+ public methods where 38+ raw-RPC methods share a 4-token template; four getter naming conventions coexist; every mutator returns `Any`. After this phase the accumulated release PR (#25) cuts 0.2.0 and glinet4-ha takes its single lockstep update (separate repo — outside this plan's tasks; the controller owns it).

## Global Constraints

- Tasks land in order; the branch merges as ONE squash whose footer enumerates the Task-2 renames (the controller writes it; each task's commit still carries its own accurate footer).
- Full gate green per task: `uv run pytest -q` (passes+skips only), `uvx prek run --all-files`, `uv run mypy glinet4` strict (zero type: ignore), coverage ≥85.
- The RPC surface is frozen: tests/test_rpc_surface.py's extracted (service, method) pair set must be IDENTICAL before and after each task (the extractor may need a path update for _routes/ — the SET may not change). The wire envelope tests must pass untouched.
- tests/test_exports.py and the README API table must be regenerated/consistent wherever names change.
- Live validation before the phase merges: the controller runs the live suite against real hardware at the end; design for it (no behavior changes beyond the enumerated renames).

## Task 1: Mixin split — glinet.py into _routes/ modules

**Files:** new `glinet4/_routes/{__init__,system,wifi,wan,clients,vpn,tailscale,services,firewall}.py`, `glinet4/glinet.py` (shrinks to composition + shared helpers), tests only where imports reference module paths.

1. Move method groups verbatim (NO signature/behavior/docstring changes — pure motion): system (router_info/status/load/unixtime/reboot/disk/usb/timezone/mac/firmware helpers), wifi (wifi_* + iface config), wan (wan_*/network_*/ethernet/connected_to_internet/ping), clients (clients_*/list_*/client_set_blocked/blocked_client_macs), vpn (wireguard_*), tailscale (tailscale_* + helpers + _tailscale_status_label), services (adguard/tor/zerotier/flow_stats/network_acceleration/led/upgrade/firmware_check), firewall (firewall_*). Each `_routes/x.py` defines `class XRoutes:` holding those methods (they use only `self._transport`, `self._payload`, `self._firmware_version` machinery — hoist shared private helpers carefully: `_payload`, firmware-version cache + `_require_firmware_version` stay on the base/glinet.py class; mixins reference them via `self.` with a Protocol or subclass-typing strategy that keeps mypy strict green WITHOUT type: ignore — the established pattern: mixins declare the shared attributes/methods via `if TYPE_CHECKING:` annotations or a shared `_RoutesBase` class. Choose ONE consistent mechanism, justify in the report).
2. `class GLinet(SystemRoutes, WifiRoutes, ..., FirewallRoutes):` — public namespace byte-compatible: `dir(GLinet)` public set identical before/after (write a throwaway comparison during the task; cite it in the report).
3. Update the rpc-surface extractor to walk `glinet4/_routes/*.py` + glinet.py; the extracted pair SET must equal the pre-split set (assert in the report).
4. Tests must pass UNCHANGED except import-path/monkeypatch-target adjustments (enumerate every test edit and why in the report — behavior edits are forbidden).

Commit: `refactor: split the route surface into _routes mixins` (not breaking; footer not needed).

## Task 2: Naming normalization, keyword-only mutators, typed mutator returns

**Files:** `glinet4/_routes/*.py`, `glinet4/glinet.py`, `glinet4/_types.py` (only if a return type needs defining), tests, README (regenerate table), CHANGELOG untouched (release-please owns it).

1. Renames (old name DELETED, no aliases — pre-1.0 clean break): `router_get_status`→`router_status`, `router_get_load`→`router_load`, `list_all_clients`→`clients_list`, `list_static_clients`→`static_clients_list`, `wifi_ifaces_get`→`wifi_ifaces`, `connected_to_internet` stays (documented Bool-returning now — see 3). Audit for any other `get_`/`list_` stragglers and normalize to the noun/noun_verb convention, enumerating every rename in the commit footer.
2. Keyword-only params on all mutators with >1 arg or any boolean arg (`client_set_blocked(mac, *, blocked)`, `flow_stats_set_enabled(*, enabled, stat_type, period)`, `wifi_iface_set_enabled(iface_name, *, enabled)`, `led_set_enabled(*, enabled)`, `tailscale_set_exit_node(*, exit_node_ip=None)`, wireguard start/stop grouping args — audit all setters). Positional single-noun args (macs, names) stay positional.
3. Typed mutator returns: replace `-> Any` on every mutator with the actual shape (usually a small TypedDict or `None` if the envelope carries nothing useful — inspect what the router returns per the unit fixtures; where genuinely unknowable without live evidence, use `dict[str, Any]` with a docstring note, NOT bare Any). `connected_to_internet` and `router_mac` get honest annotations (`bool` / `str` — they already reshape; verify).
4. Hide the recursion `depth` params on tailscale_start/stop (private `_depth` or an internal helper — public signature takes none).
5. Update ALL tests for renames/kw-only (mechanical), regenerate the README API table, keep tests/test_exports.py green.

Commit: `fix!: normalize the route API — renames, keyword-only mutators, typed returns` with a complete footer enumerating every rename and signature change.

## Task 3: Parametrize the pattern-A/B tests over a route table

**Files:** tests only (`tests/test_glinet_unit.py` and/or a new `tests/test_routes_table.py`).

1. Build a declarative table (name, route pair, unwrap key or None, sample payload, expected return) for the ~38 trivial pattern-A/B getters; ONE parametrized test drives them all (replacing the copy-paste per-method tests — DELETE the superseded ones, keep any test asserting something beyond the template). Orchestration-method tests stay as-is.
2. Net test count may drop; coverage must NOT drop below the pre-task number (report both).

Commit: `test: drive the trivial route getters from a parametrized table`.
